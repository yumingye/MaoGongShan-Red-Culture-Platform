# 工程化整理与打包报告

检查日期：2026-07-28

## 目录与配置

- 前端、后端、文档和脚本已经分离。
- 后端配置集中到 `backend/config.py`，自动读取 `backend/.env`。
- 数据库和上传目录的相对路径统一以项目根目录解析。
- 前端 API 地址统一由 `VITE_API_BASE_URL` 和 Vite 代理管理。
- 源代码未发现用户本机绝对路径、真实 API Key 或 Token。
- `.gitignore` 已排除依赖、虚拟环境、缓存、日志、私密文档和真实环境文件。

## 依赖与构建

- Python 3.12 干净虚拟环境安装通过。
- `npm install` 干净安装通过。
- npm 安全审计：0 个已知漏洞。
- Vue 3.5.40、Vite 8.1.5、`@vitejs/plugin-vue` 6.0.8。
- Vite 生产构建通过，共转换 1753 个模块。
- Vite 8 函数式 `manualChunks` 分包配置验证通过。

## 功能与数据

- 后端健康检查和 Swagger 文档返回 200。
- 首页、党史、党史详情、地图、问答和 404 深层路由返回正常。
- 全量质量脚本通过：0 个提醒，0 个失败。
- 125 条图库记录均有可显示照片。
- 53 篇党史文章、65 条章节媒体、30 份人物档案、9 个历史阶段和 12 个图文微课通过一致性检查。
- 无高德地图 Key 时显示本地导览；无大模型 Key 时使用本地知识库检索问答。

## 一键脚本

- `start.bat` 可自动选择 `py -3` 或 `python`。
- 依赖清单未变化时不会重复安装。
- 启动后记录前端和后端的服务 PID、窗口 PID 与启动时间。
- `stop.bat` 已验证关闭全部四个记录进程。
- 停止后 8000、5173 端口监听为 0，项目 Python 进程为 0。

## 发布包

- 最终目录不包含 `node_modules`、`.venv`、`dist`、缓存、日志或 `.env`。
- ZIP 仅包含一层 `maogongshan-red-culture-platform` 根目录。
- 公开数据库保留文化资源数据，`chat_records` 和 `visit_records` 已清空。
- 未发现超过 GitHub 100 MB 普通上传限制的单文件。
