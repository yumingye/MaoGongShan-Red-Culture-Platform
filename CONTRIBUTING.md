# 贡献指南

感谢参与毛公山红色文化数字资源平台建设。提交改动前，请确保内容来源清晰、图片授权说明完整，并且不包含个人隐私、真实密钥或未获授权的大型媒体文件。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 按 README 配置前后端环境，不提交真实 `.env`。
3. 保持现有 API、数据字段和页面路径兼容；功能变更应在 Pull Request 中说明。
4. 在 `frontend` 目录运行 `npm run lint`、`npm run test` 和 `npm run build`。
5. 提交 Pull Request，说明改动目的、验证结果、数据来源和界面变化。

## 内容规范

- 地方史实、全国党史拓展、当代实景和项目自制图解必须明确区分。
- 新增历史材料应记录来源名称、来源链接、核验状态和版权说明。
- 新增图片放入根目录 `assets/`，不要直接提交同步生成的 `frontend/public/assets/`。
- 数据库结构变更应保持迁移幂等，并在 `docs/` 中记录影响。

发现问题时，请提供复现步骤、预期结果、实际结果和运行环境；涉及安全或隐私的问题请按 `SECURITY.md` 私下报告。
